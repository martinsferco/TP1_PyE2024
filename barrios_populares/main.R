# Para que funcione la descarga de datos, tenemos que abrir este archivo con doble click 
# sobre el mismo, para que el working directory se setee en la carpeta en la que se 
# encuentra guardado. Si abrimos R y luego el archivo, el working directory se setea 
# en una ruta inicial en la que no podemos acceder el archivo "Datos.xlsx"
source("descarga_datos.R")
source("limpieza_tabla.R")
source("recursos_graficos.R")
source("analisis_numerico.R")

